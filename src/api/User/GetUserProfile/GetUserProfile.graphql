type GetUserProfileResponse {
  ok: Boolean!
  error: String
  user: User
  likeCount: Int!
  myLike: Like
  existingRecommend: Recommend
}

type Query {
  GetUserProfile(id: Int!): GetUserProfileResponse!
}
